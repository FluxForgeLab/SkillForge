---
description: Python 后端（skillforge 包、tests、demo 脚本）编码约定
globs: skillforge/**/*.py,tests/**/*.py,demo/**/*.py
alwaysApply: false
---

# Python Backend

- Python 3.12，`uv` 管理依赖，`ruff` 负责 lint + format（line-length 100）。所有公共函数带类型注解。
- 领域对象一律 pydantic v2 `BaseModel`（`skillforge/domain/`）；跨模块传递用领域对象，不传裸 dict。
- 数据库：`sqlite3` + `skillforge/db/schema.sql`，仓储函数放 `skillforge/db/repositories/`，SQL 用参数绑定，禁止字符串拼接。
- 异步：I/O 边界（模型调用、docker、HTTP）用 `async`；纯计算保持同步。FastAPI handler 全部 `async def`。
- 模型调用只通过 `ModelGateway.generate(ModelRequest)`；需要结构化输出用 `models/structured.py`，不要手写 JSON 解析重试。
- 每个核心阶段 emit `TraceEvent`（`skillforge/tracing/`），事件类型只用设计文档 §19 列出的枚举。
- 状态变更走 `domain` 中的合法转移表，直接改 `status` 字段视为 bug。
- 配置只从 `skillforge/config.py` 的 `Settings` 读取，不在模块里直接 `os.environ`。
- 错误：定义 `SkillForgeError` 子类；不要 `except Exception: pass`；policy 违规抛 `PolicyViolation` 并 emit 事件。
- 日志用 `logging.getLogger(__name__)`，不用 print（CLI 输出除外）。

## 目录归属

| 关注点 | 位置 |
|---|---|
| Sandbox 接口/实现 | `skillforge/sandbox/` |
| Agent loop、tools、skill loader | `skillforge/runtime/` |
| ops-lab 白名单操作 | `skillforge/runtime/tools/opslab.py` |
| 评测、断言、A/B、Benchmark | `skillforge/evaluator/` |
| Failure / Patch / Gate | `skillforge/evolution/` |
| 路由 | `skillforge/api/routers/<resource>.py` |

## 测试

- 单测用 `FakeModelAdapter` 与 `tmp_path`；不 mock 自己的领域对象。
- 集成测试标记 `@pytest.mark.integration`，`conftest` 提供 `ops_lab` fixture（up → yield → reset）。
- 生成物（SKILL.md、BENCHMARK.md）用快照文件对比，放 `tests/fixtures/snapshots/`。
