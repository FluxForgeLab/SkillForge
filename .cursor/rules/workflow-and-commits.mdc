---
description: 按执行方案逐 commit 开发的 Human Gate、验证与提交约定
alwaysApply: true
---

# Workflow & Commits

## 默认工作单位 = 执行方案中的一个 commit

对 `docs/execution-plan.md` 中的 C 编号，默认流程：

```text
align-plan-step
      ↓
HUMAN GATE
      ↓
implement-plan-step
      ↓
review-plan-step
      ↓
HUMAN COMMIT GATE
      ↓
git commit
```

详细 Gate 规则见 `human-control-loop.mdc`。

用户明确说“跳过对齐 / 直接实现”或“跳过 review / 直接提交”时可以旁路对应 Gate；Agent 不得自行假设旁路。

## Scope

1. 开始前读对应执行方案条目的“内容”和“完成标准”。
2. 只做该条目范围内的事。范围外问题记到回复末尾，不顺手改。
3. 实现 + 该功能点的测试 + 必要文档更新属于同一个逻辑变更。
4. 完成标准不可达时说明原因，不降低标准。
5. Preflight 后如果实现需要越出批准的 Change Radius，先停下来重新过 Human Gate。

## 何时执行 git commit

准备好变更和 commit message，但默认不要执行 `git commit`。

执行 commit 必须满足其一：

- review 已完成，且用户之后明确要求提交；
- 用户明确要求跳过 review 并直接提交。

以下都不是提交授权：

- “测试通过”；
- “完成标准满足”；
- “实现完成”；
- execution-plan 里存在 commit message。

## 提交前必须通过

```bash
uv run ruff check . && uv run ruff format --check . && uv run pytest -m "not integration"
# 改了 apps/web 时追加
pnpm -C apps/web lint && pnpm -C apps/web build
# 改了 demo/ops-lab、sandbox、runtime tools、evaluator 时追加（需要 Docker）
uv run pytest -m integration
```

## Commit message

执行方案条目：

```text
<type>(<scope>): <summary>

Plan: C3.5
<可选：取舍、调参结论、已知限制>
```

- type ∈ `feat|fix|test|docs|chore|refactor|spike`；scope 用模块名。
- 一个 C 编号一个逻辑 commit，不把两个 C 编号混在一起。
- `spike` commit 必须在 body 写数字、结论和依据。
- 开发 Harness / repo maintenance 等明确不属于 execution-plan 的变更可以使用 `chore(...)`，body 说明它是 meta change，不伪造 Plan 编号。

## 测试约定

- `tests/unit/`：不依赖 Docker 和真实模型，用 `FakeModelAdapter`。
- `tests/integration/`：标记 `@pytest.mark.integration`，依赖 ops-lab。
- 真实模型响应录制到 `tests/fixtures/transcripts/` 后回放。
- 修 bug 先写复现测试。

## 不确定时

先查设计文档与执行方案。两者没答案、且不同选择会改变 Contract 或明显工作量时才提问；否则按最简单且不违反安全边界的方案，并显式记录假设。

连通性 / 打不开问题先走 `diagnose-listen`，不要先讲原因。
